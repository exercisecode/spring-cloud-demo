
spring:
    application: 
        name: spring-cloud-base
    profiles:
        active: cloud    
    cloud:
        config:
            enabled: false 
            uri: ${SPRING_CONFIG_URI:http://127.0.0.1:30000/}
            